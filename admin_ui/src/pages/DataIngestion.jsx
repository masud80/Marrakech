import { Card, CardContent, Typography } from '@mui/material';

export default function DataIngestion() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h4" gutterBottom>
          Data Ingestion
        </Typography>
        <Typography>
          This is the Data Ingestion page.
        </Typography>
      </CardContent>
    </Card>
  );
} 