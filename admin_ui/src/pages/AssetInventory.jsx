import { Card, CardContent, Typography } from '@mui/material';

export default function AssetInventory() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h4" gutterBottom>
          Asset Inventory
        </Typography>
        <Typography>
          This is the Asset Inventory page.
        </Typography>
      </CardContent>
    </Card>
  );
} 