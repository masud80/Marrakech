import { Card, CardContent, Typography } from '@mui/material';

export default function MLModelStatus() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h4" gutterBottom>
          ML Model Status
        </Typography>
        <Typography>
          This is the ML Model Status page.
        </Typography>
      </CardContent>
    </Card>
  );
} 